import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

const store = new Vuex.Store({
  modules: {},
  state: {
    loading: true
  },
  actions: {},
  mutations: {},
  getters: {}
});

export default store;
